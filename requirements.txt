# OllamaVision Benchmark Suite v3.0
# Install with: pip install -r requirements.txt
# On Jetson AGX Orin:  pip install -r requirements.txt --break-system-packages

requests>=2.31.0
rich>=13.7.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0

# Jetson-stats (real Jetson hardware monitoring, install separately):
# sudo pip install -U jetson-stats
# jetson-stats>=4.2.0
